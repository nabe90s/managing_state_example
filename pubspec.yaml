name: managing_state_example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.14.0 <3.0.0'
  flutter: '>=2.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_redux: ^0.8.2
  flutter_mobx: ^2.0.0
  mobx: ^2.0.1
  flutter_riverpod: ^1.0.3
  equatable: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.2
  mobx_codegen: ^2.0.1+3

flutter:
  uses-material-design: true
